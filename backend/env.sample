# backend/.env.sample
# 1. Copy this file to .env in the same directory
# 2. Replace the placeholder values with actual credentials you received
# 3. Run docker compose up --build

DEBUG=True
DEBUG_PDF=True
SECRET_KEY="replace-with-provided-key"
DB_NAME="replace-with-provided-name"
DB_USER="replace-with-provided-user"
DB_PASSWORD="replace-with-provided-password"
DB_HOST="replace-with-provided-host"
DB_PORT="replace-with-provided-port"
MICROSOFT_GRAPH_KEY="replace-with-provided-key"
MICROSOFT_GRAPH_SECRET="replace-with-provided-secret"
MICROSOFT_TENANT_ID="replace-with-provided-tenant-id"
MICROSOFT_BACKEND_REDIRECT_URL="http://localhost:8000/complete/microsoft-graph"
MICROSOFT_FRONTEND_REDIRECT_URL="http://localhost:3000/auth/microsoft/callback"
